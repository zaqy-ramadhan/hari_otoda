
    // JavaScript to toggle play/stop icon (same as original page)
    const playIcon = document.getElementById("play-icon");

    let isPlaying = false;

    //playIcon.addEventListener("click", function () {
    //    if (isPlaying) {
    //        playIcon.classList.remove("fa-stop");
    //        playIcon.classList.add("fa-play");
    //    } else {
    //        playIcon.classList.remove("fa-play");
    //        playIcon.classList.add("fa-stop");
    //    }
    //    isPlaying = !isPlaying;
    //});
//
    // JavaScript to toggle switch icon and navigate to a new page (initially on the other side)
   // const switchIcon = document.getElementById("switch-icon");
//
   // let isSwitchOn = true; // Set the switch to on by default on the new page
//
   // switchIcon.addEventListener("click", function () {
   //     if (isSwitchOn) {
   //         // Go back to the previous page
   //         window.history.back();
   //     } else {
   //         // Navigate to a new page (you can change the URL)
   //         window.location.href = "navbar.html"; // Update with the original page URL
   //     }
   //     isSwitchOn = !isSwitchOn;
   // });